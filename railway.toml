[build]
builder = "NIXPACKS"

[build.buildpacks]
python = "*"

[deploy]
startCommand = "uvicorn model_api:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
